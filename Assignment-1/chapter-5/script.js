/* 1. Write a program that take two numbers & add them in a
new variable. Show the result in your browser */
let bookPrice = 50;
let penPrice = 16;
let plus = bookPrice + penPrice;
console.log(`Plus of 50 and 16 is ${plus}`); // 66


/* 2. Repeat task1 for subtraction, multiplication, division &
modulus. */
let num1 = 20;
let num2 = 5;

// Subtraction
let subtraction = num1 - num2;
console.log(`Subtraction of ${num1} and ${num2} is ${subtraction}`); // -5

// Multiplication
let multiplication = num1 * num2;
console.log(`Multiplication of ${num1} and ${num2} is ${multiplication}`); // 100)

// Division
let division = num1 / num2;
console.log(`Division of ${num1} and ${num2} is ${division}`); // 4

// Modulus
let modulus = num1 % num2;
console.log(`Modulus of ${num1} and ${num2} is ${modulus}`); // 0



/* 3. Do the following using JS Mathematic Expressions
a. Declare a variable. */
let x;

// b. Show the value of the variable in your browser
console.log(`Value after variable declaration is:  + ${x}`);

// c. Initialize the variable with some number.
x = 5;

// d. Show the value after initialization.
console.log(`Initial value: + ${x}`);

// e. Increment the variable.
x++;

// f. Show the value after increment.
console.log(`Value after increment is: + ${x}`);

// g. Add 7 to the variable.
x = x + 7;

// h. Show the value after addition.
console.log(`Value after addition is: + ${x}`);

// i. Decrement the variable.
x--;

// j. Show the value after decrement.
console.log(`Value after decrement is: + ${x}`);

// k. Show the remainder after dividing the variable’s value by 3.
var remainder = x % 3;

// l. Output the remainder.
console.log(`Remainder is: + ${remainder}`);



/* 4. Cost of one movie ticket is 600 PKR. Write a script to
store
ticket price in a variable & calculate the cost of buying 5
tickets
to a movie. Example output: */
let ticketPrice = `600`;
let totalCost = ticketPrice * 5;
console.log(`The cost of buying 5 movie tickets is ${totalCost} PKR`); // 3000



/* 5. Write a script to display multiplication table of any
number in your browser. E.g */
let number = 5;
console.log(`${number} x 1 = ${number * 1}`);
console.log(`${number} x 2 = ${number * 2}`);
console.log(`${number} x 3 = ${number * 3}`);
console.log(`${number} x 4 = ${number * 4}`);
console.log(`${number} x 5 = ${number * 5}`);
console.log(`${number} x 6 = ${number * 6}`);
console.log(`${number} x 7 = ${number * 7}`);
console.log(`${number} x 8 = ${number * 8}`);
console.log(`${number} x 9 = ${number * 9}`);
console.log(`${number} x 10 = ${number * 10}`);



// Added message instructing users to inspect the console for output.
alert(" Please inspect the console (press F12) to view the output, 🔍 \n and don't forget to check the code as well! 💻 \n Thank you for checking my assignment! 😊");